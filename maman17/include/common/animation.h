#ifndef ANIMATION_H
#define ANIMATION_H

// Returns the current animation frame index
int getAnimationFrame();

// Increments the animation frame index
void incrementAnimationFrame();

// Returns the total number of animation frames
int getTotalAnimationFrames();

#endif